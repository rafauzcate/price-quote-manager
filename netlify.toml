[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18"

# SPA fallback so deep links like /app/quotes resolve correctly
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
